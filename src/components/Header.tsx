import React, { ReactElement } from 'react'

export default function Header(): ReactElement {
  return (
    <h1
      className='Header-title'
    >
      Expense Tracker/ Cash Book
    </h1>
  )
}

